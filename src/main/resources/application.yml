spring:
  profiles:
    active: prod

---
spring:
  profiles: dev
  config:
    location:
server:
  port: 8083
jobs:
  scheduled:
    cron: 0/10 * * * * ?

---
spring:
  profiles: prod
server:
  port: 8084
jobs:
  scheduled:
    cron: 0/10 * * * * ?